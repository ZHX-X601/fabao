2026-09-12
- 用户询问后端一键启动方式。项目结构：backend/（FastAPI + MySQL，含 .venv 和 .env），根目录为 Vue3 + Vite 前端。
- 已在项目根目录创建 `start-backend.bat`：cd 到 backend、激活 .venv、运行 `uvicorn app.main:app --reload --port 8000`，双击即用。
- 已将 `start-backend.bat` 追加到 `.gitignore` 的"本地工具脚本"排除区（同区还有 git-push-fabao.bat）。
- 前后端联调配置：axios baseURL 在 `src/utils/http.js`（/api/v1），实际后端地址在 `vite.config.js` 的 proxy（dev only）。
- 线上部署信息：前端部署在 CF Pages，自定义域名 `https://fabao.blog-x.cc.cd`；后端通过花生壳（贝锐）内网穿透暴露为 `https://954487onpr52.vicp.fun`（隧道通，uvicorn 有响应）。
- 已实测修复跨域：后端 CORS_ORIGINS 增加线上域名后，OPTIONS 预检从 400 Disallowed CORS origin 变为放行。注意 .env 改动需手动重启后端（--reload 只监听 py 文件）。
- 跨域问题最终定位与修复（19:44 验证通过）：
  - 现象：线上页面登录报 CORS "No 'Access-Control-Allow-Origin'"，本地探测隧道 OPTIONS 返回 400 Disallowed CORS origin。
  - 真因：改完 .env 后后端未真正用上新配置 —— 机器上有 6 个残留 python 进程（两组多开），占用 8000 的实例仍是旧配置。
  - 修复：① .env 补 `https://fabao.blog-x.cc.cd`；② `app/config.py` 的 env_file 由相对 `.env` 改为绝对路径 `BASE_DIR/".env"`，避免从其它目录启动读不到 .env；③ `app/main.py` lifespan 增加 `CORS 允许来源` 启动日志便于排查；④ 杀掉全部残留进程后单实例重启。验证：OPTIONS 返回 200 且带 ACAO；POST /api/v1/auth/login 经隧道返回业务响应 400「用户名或密码错误」。
  - 排查经验：Windows 下 curl -d 带转义双引号会失败，改用 `-d "@文件"`；PowerShell 5.1 无 `-SkipHttpErrorCheck`。
- 排查"CF Pages 部署后登录 405"：根因是 Vite proxy 仅开发环境生效，生产构建后 /api 请求打到 Pages 静态资源返回 405。已改 `src/utils/http.js` 支持 `VITE_API_BASE_URL` 环境变量（设置后拼完整后端地址，未设置走 /api/v1 代理）。
- 应用户要求在项目根目录生成《前后端联调与部署机制讲解.md》
- 接入 FastGPT 文书生成（appId=6aa606950a70c2c2ec1becc5）和合同审查（appId=6aa6090d0a70c2c2ec1bef40）：
  - config.py 新增 FASTGPT_DOC_APP_ID / FASTGPT_CONTRACT_APP_ID
  - document_service.py 从模板拼接改为调用 FastGPT（降级仍保留本地模板）
  - contract_service.py 从硬编码模拟改为调用 FastGPT 并解析 JSON（兼容 markdown 包裹、降级仍保留模拟）
  - 前端 DocGenerate.vue 文书类型改为：民事起诉状/民事答辩状/律师函/授权委托书，字段匹配 FastGPT 提示词
  - 前端 ContractReview.vue 新增法律依据（law）字段展示，合规条款改为条件显示
  - schemas/document.py DOC_TYPES 同步更新
  - 后端 review API 改为 async 以匹配异步 FastGPT 调用
- 修复合同上传文件格式限制：后端 `file_handler.py` ALLOWED_SUFFIXES 从仅 {.docx, .pdf} 扩展为 {.docx, .pdf, .txt, .doc, .wps}，新增 `extract_text_from_txt()` 支持 UTF-8/GBK 自动检测，.doc 旧格式暂不支持提取并给出提示。前端 ContractReview.vue accept 同步扩展。
- 修复合同审查接口 500 错误：`contract.py` 第 81 行 `review_contract()` 是 async 函数但调用时缺少 `await`，导致 coroutine 对象被直接 JSON 序列化报 `Object of type coroutine is not JSON serializable`。加 `await` 后验证通过（降级模式返回 score=72, 3 条风险）。
- 实现合同审查报告下载（.docx）：
  - 新增 `backend/app/services/report_service.py`：使用 python-docx 生成 Word 报告（标题、评分、结论、每条风险含原文/分析/建议/法律依据、免责声明）。关键：必须设置中文字体 `eastAsia = Microsoft YaHei`，否则 Word 打开中文变方块。
  - 新增 `backend/app/api/contract.py` 接口 `GET /contracts/report/{contract_id}`：返回 `StreamingResponse`，`Content-Disposition` 用 RFC 5987 双 filename（中文 UTF-8 编码），`Access-Control-Expose-Headers: Content-Disposition` 暴露给前端。
  - 前端 `src/api/contract.js` 新增 `downloadContractReport(id)`：`responseType: 'blob'`，依赖 http.js 第 42 行 blob 短路逻辑。
  - 前端 `ContractReview.vue` 重写 `handleDownloadReport`：解析 Content-Disposition 的 `filename*=UTF-8''...` 拿中文文件名 → `URL.createObjectURL` + `<a download>` 触发下载 → `URL.revokeObjectURL` 释放 → 按钮加 :loading 防重复点击。
  - 实测验证：返回 200，40KB docx，Content-Disposition 带 `test_contract_审查报告.docx`（URL 编码），python-docx 反解析 49 个段落正常。
