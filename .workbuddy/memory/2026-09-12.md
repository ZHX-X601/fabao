2026-09-12
- 用户询问后端一键启动方式。项目结构：backend/（FastAPI + MySQL，含 .venv 和 .env），根目录为 Vue3 + Vite 前端。
- 已在项目根目录创建 `start-backend.bat`：cd 到 backend、激活 .venv、运行 `uvicorn app.main:app --reload --port 8000`，双击即用。
- 已将 `start-backend.bat` 追加到 `.gitignore` 的"本地工具脚本"排除区（同区还有 git-push-fabao.bat）。
- 前后端联调配置：axios baseURL 在 `src/utils/http.js`（/api/v1），实际后端地址在 `vite.config.js` 的 proxy（dev only）。
- 线上部署信息：前端部署在 CF Pages，自定义域名 `https://fabao.blog-x.cc.cd`；后端通过花生壳（贝锐）内网穿透暴露为 `https://954487onpr52.vicp.fun`（隧道通，uvicorn 有响应）。
- 已实测修复跨域：后端 CORS_ORIGINS 增加线上域名后，OPTIONS 预检从 400 Disallowed CORS origin 变为放行。注意 .env 改动需手动重启后端（--reload 只监听 py 文件）。
- 排查"CF Pages 部署后登录 405"：根因是 Vite proxy 仅开发环境生效，生产构建后 /api 请求打到 Pages 静态资源返回 405。已改 `src/utils/http.js` 支持 `VITE_API_BASE_URL` 环境变量（设置后拼完整后端地址，未设置走 /api/v1 代理）；后端 CORS_ORIGINS 需加 Pages 域名（backend/.env.example 已加注释）。后端尚无公网部署。
