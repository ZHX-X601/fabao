# 法宝项目：前端是怎么调到后端的 —— 本地预览、CF Pages 部署、跨域全链路讲解

> 本文面向"代码是 AI 写的、但想搞懂原理"的你。读完你就明白：
> 为什么本地能跑、传到 CF Pages 就连不上后端、405 和跨域是怎么来的、花生壳在其中扮演什么角色。
> 不需要任何前置知识，从请求发出那一刻讲起。

---

## 0. 太长不看版：两张图对比

**本地开发（`npm run dev`）——一切顺利：**

```
浏览器                          你电脑上的进程
┌──────────────┐   请求 /api/v1/auth/login    ┌─────────────────────┐
│ localhost:5173│ ──────────────────────────▶ │ Vite 开发服务器 (5173)│
│ 上的页面      │ ◀────────────────────────── │   ↓ 检测到 /api 开头  │
└──────────────┘        页面/数据             │   ↓ 悄悄转发          │
                                             └─────────┬───────────┘
                                                       ▼
                                             ┌─────────────────────┐
                                             │ FastAPI 后端 (8000) │
                                             └─────────────────────┘
```

关键点：**浏览器以为自己在跟 5173 说话**，Vite 在背后当"二传手"把 `/api` 请求转给 8000。
浏览器视角这是"同源请求"，所以**根本不存在跨域问题**。

**线上（CF Pages + 花生壳）——环环相扣，一环没配就挂：**

```
浏览器                              公网                         你家里
┌───────────────────┐   ① 加载页面    ┌──────────────┐            ┌──────────────┐
│ https://fabao.     │ ◀──────────▶ │ Cloudflare   │ 静态文件    │  你的电脑     │
│ blog-x.cc.cd      │               │ Pages CDN    │            │              │
│                   │   ② 请求 API   └──────────────┘            │ ②' 花生壳客户端│
│                   │ ───────────────▶ https://954487onpr52.vicp.fun            │
└───────────────────┘      (跨域！需要后端 CORS 放行)             │      ↓       │
                                                            │ FastAPI:8000 │
                                                            └──────────────┘
```

这次遇到的 405 → 网络异常 → 跨域报错，就是这条链路上三个不同环节分别出了问题。下面逐个讲。

---

## 1. 先建立全局认知：这个项目里有两个"服务"

| | 前端 | 后端 |
|---|---|---|
| 技术 | Vue 3 + Vite + Element Plus | Python FastAPI + MySQL |
| 入口命令 | `npm run dev` / `npm run build` | `uvicorn app.main:app --port 8000` |
| 本质 | **一堆静态文件**（HTML + JS + CSS） | 一个**持续运行的程序**，监听端口等请求 |
| 部署要求 | 任何能放静态文件的地方（CF Pages、Nginx、OSS） | 必须有一台**一直开着机**的服务器跑它 |

> **最重要的认知**：`npm run build` 之后，前端就是 `dist/` 目录里的一堆文件，没有"进程"概念。
> 它能"调接口"，纯粹是因为**浏览器**在用户电脑上执行了其中的 JS 代码，由浏览器发起 HTTP 请求。
> 所以"前端部署在哪"和"后端部署在哪"是完全独立的两件事，中间只靠一个 URL 连接。

---

## 2. 请求是从代码里哪里发出来的

所有请求都经过一个统一封装：`src/utils/http.js`（axios 实例）。

```js
const baseURL = import.meta.env.VITE_API_BASE_URL
  ? `${import.meta.env.VITE_API_BASE_URL}/api/v1`
  : '/api/v1'

const http = axios.create({
  baseURL,               // ← 决定请求打到哪的关键
  timeout: 120000
})
```

业务页面里调用 `http.post('/auth/login', {...})` 时，实际发出的完整地址是：

```
{baseURL}/auth/login
```

那 `baseURL` 到底是什么值？取决于**构建时**有没有 `VITE_API_BASE_URL` 这个环境变量：

- **没设置**（本地开发）→ `baseURL = '/api/v1'`，是相对路径，请求发给"当前页面所在的那个服务器"
- **设置了**（CF Pages 上配了）→ `baseURL = 'https://954487onpr52.vicp.fun/api/v1'`，绝对地址，直接打到花生壳隧道

> **关于 `import.meta.env` 的一个关键特性**：它是**编译时替换**的，不是运行时读取！
> Vite 打包时会把 `import.meta.env.VITE_API_BASE_URL` 这段代码直接替换成字符串常量写死进 JS 文件。
> 所以：**在 CF Pages 上改环境变量后，必须重新部署（触发重新 build）才生效**，只改配置不重新构建等于没改。

---

## 3. 本地预览为什么能连上后端：Vite 代理

`vite.config.js` 里这段是核心：

```js
server: {
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://localhost:8000',  // ← 后端地址在这改
      changeOrigin: true
    }
  }
}
```

工作流程：

1. 你访问 `http://localhost:5173`，Vite 开发服务器把页面给你
2. 页面 JS 请求 `/api/v1/auth/login`（相对路径）→ 发给 5173
3. Vite 看到路径以 `/api` 开头 → **不返回静态文件，而是原样转发**给 `http://localhost:8000/api/v1/auth/login`
4. 后端响应 → Vite 把响应转回给浏览器

**为什么这样就没有跨域？**
因为跨域是**浏览器**的行为（见第 6 节），浏览器只看"页面从哪来、请求发到哪"。
页面来自 5173，请求也发给 5173 → 同源 → 浏览器完全不干涉。
至于 Vite 转发给 8000，那是**服务器与服务器之间**的通信，浏览器不知道也不关心。

> ⚠️ **一个容易混淆的坑**：`npm run dev` 和 `npm run preview` 是两回事！
> - `npm run dev`：开发服务器，带上面这个代理 ✅
> - `npm run preview`：预览 `npm run build` 的产物，**默认不加载 `server.proxy`**（那是另一套 `preview.proxy` 配置，本项目没配）
> 所以本地 `build` 完直接 `preview` 会连不上后端——这其实和线上遇到的问题同根源：**proxy 不进打包产物**。

---

## 4. CF Pages 为什么先报 405：静态托管不认识 POST

部署到 CF Pages 发生了什么：

1. `git push` → CF Pages 自动触发构建：`npm run build` → 得到 `dist/`
2. `dist/` 被推到 Cloudflare 的全球 CDN
3. 用户访问 `https://fabao.blog-x.cc.cd`，拿到的是 CDN 上的静态文件

**问题出在**：当时前端没配 `VITE_API_BASE_URL`，`baseURL` 还是相对路径 `/api/v1`。
于是浏览器老老实实把请求发给了 **CF Pages 自己**：

```
POST https://fabao.blog-x.cc.cd/api/v1/auth/login
```

CF Pages 是静态文件托管：它只会"找文件、发文件"。`/api/v1/auth/login` 这个路径下没有文件，而且对静态资源发 POST 方法是不被允许的 → 返回 **405 Method Not Allowed**。

**解法**（已实施）：在 CF Pages 项目设置里加构建环境变量：

```
VITE_API_BASE_URL = https://954487onpr52.vicp.fun
```

这样打包出的 JS 里 baseURL 变成完整地址，请求不再发给 CF Pages，而是直接发给花生壳域名。
注意格式三要求：`https://` 开头、**不带** `/api/v1`（代码会自己拼）、**末尾不带** `/`。

---

## 5. 花生壳是什么：让家里的电脑"被公网访问"

你的后端跑在家里电脑的 `127.0.0.1:8000`，而家里宽带通常没有公网 IP（或运营商不给开放端口），
公网上的用户没法直接访问它。花生壳（内网穿透）就是解决这个的：

```
公网用户 ──▶ 954487onpr52.vicp.fun（花生壳云端服务器）
                    │
                    │  ② 云端把请求沿"隧道"下发
                    ▼
             家里电脑上的花生壳客户端（它主动和云端保持长连接）
                    │
                    │  ③ 客户端转发到本机
                    ▼
             127.0.0.1:8000（FastAPI 后端）
```

要点：

- 隧道是**客户端主动向外建立**的长连接，所以**不需要公网 IP、不需要路由器端口映射**
- 对外暴露的 `https://954487onpr52.vicp.fun` 就是云端入口，流量经隧道绕回你家
- 免费版限制：带宽很小、域名随机（重启客户端可能变）、有流量限额——演示课设够用，生产不行
- **前提**：你家里这台电脑必须开机、后端必须运行、花生壳客户端必须在线，三者缺一个线上就挂

---

## 6. CORS 跨域：这次真正的"元凶"

### 6.1 什么是同源、什么是跨域

浏览器的**同源策略**：协议 + 域名 + 端口，三者**完全一致**才算同源。

| 页面地址 | 请求地址 | 是否跨域 |
|---|---|---|
| `http://localhost:5173` | `http://localhost:5173/api/...` | 否（走代理时就是这样） |
| `https://fabao.blog-x.cc.cd` | `https://954487onpr52.vicp.fun/api/...` | **是**（域名不同） |

### 6.2 跨域被拦的真实机制（很多人理解错了）

跨域**不是"请求发不出去"**，流程是这样：

1. 我们的登录请求带 `Content-Type: application/json`（和 `Authorization` 头），属于**非简单请求**
2. 浏览器在真正发 POST 之前，先偷偷发一个 **OPTIONS 请求**（叫**预检请求 preflight**），问后端："我这个来源、我要用这些方法和头，你允许吗？"
3. 后端回复的响应头里必须带 `Access-Control-Allow-Origin: https://fabao.blog-x.cc.cd` 才算"批准"
4. 预检通过 → 浏览器才发真正的 POST；预检不通过 → **浏览器直接判死刑**，页面 JS 只收到一个无响应的错误

这就是为什么 F12 里报的是 CORS 错误、而前端 `http.js` 里走到了"网络异常"那个分支——
对 axios 来说，这就是一个"连 HTTP 响应都没有"的失败。

### 6.3 后端是怎么"批准"的：CORS 白名单

`backend/app/main.py`：

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.CORS_ORIGINS,  # ← 白名单，来自 .env
    allow_credentials=True,
    ...
)
```

`backend/.env`：

```
CORS_ORIGINS=["http://localhost:5173","http://localhost:5174","https://fabao.blog-x.cc.cd"]
```

最初的报错就是因为白名单里只有前两个 localhost，没有线上域名，
后端对预检请求回了 `400 Disallowed CORS origin`，浏览器于是拦截。

**为什么本地开发从没遇到 CORS？** 因为本地走了 Vite 代理，请求对浏览器来说是同源的，**CORS 机制根本没被触发**。这也是"本地没问题、一上线就出事"的根本原因——两条链路根本不同。

**排查技巧**：CORS 是纯浏览器行为，curl / Postman 没有同源策略。所以怀疑跨域时直接：

```powershell
curl.exe -s -i -X OPTIONS "https://954487onpr52.vicp.fun/api/v1/auth/login" -H "Origin: https://fabao.blog-x.cc.cd" -H "Access-Control-Request-Method: POST"
```

返回 200 且带 `access-control-allow-origin` 头 = 放行；返回 400 = 白名单没配对。不依赖浏览器就能验证。

---

## 7. 当天踩的第三个坑：多实例"假重启"

现象：`.env` 改了、也"重启"了，线上依然报跨域。

真相：电脑上残留了 **6 个 python 进程**（其实是**两组**后端实例，因为 `uvicorn --reload` 一次启动就是一个"父进程 + 工作进程"的小团体）。
占着 8000 端口、真正对外服务的那个实例用的还是**旧配置**。

为什么会残留：Windows 上直接点 **X 关窗口** 或 Ctrl+C 偶尔杀不干净，进程变成后台孤儿继续跑；
之后新开的实例可能端口被占启动失败，报错一闪而过没看见，造成"我重启了却没生效"的假象。

为此做了两个加固（已写进代码）：

1. `app/config.py`：读 `.env` 从相对路径改成**绝对路径**——以前从非 backend 目录启动会读不到 `.env` 而悄悄用默认值
2. `app/main.py`：启动日志会打印 `CORS 允许来源：[...]`——一眼看出配置有没有生效

---

## 8. 配置速查表：想改什么 → 动哪里 → 还要做什么

| 需求 | 改哪里 | 改完必须做什么 |
|---|---|---|
| 后端换了端口（本地开发） | `vite.config.js` 的 `proxy.target` | 重启 `npm run dev` |
| 前端请求的后端地址（线上） | CF Pages 环境变量 `VITE_API_BASE_URL` | **重新部署**（重新构建才生效） |
| 允许某个域名跨域访问后端 | `backend/.env` 的 `CORS_ORIGINS` | **重启后端**（`--reload` 不监听 .env） |
| 改后端代码 | `backend/app/` 下任意 `.py` | 不用管，`--reload` 自动重启 |
| 数据库账号密码 | `backend/.env` 的 `DB_*` | 重启后端 |
| 花生壳映射目标 | 花生壳客户端里的映射配置 | 一般即时生效，不行就重启客户端 |

## 9. 启动 / 排查命令速查

```powershell
# ===== 启动 =====
# 后端（推荐，双击也行）
start-backend.bat
# 前端
npm run dev

# ===== 排查 =====
# 看看 8000 端口被谁占了
Get-NetTCPConnection -LocalPort 8000 -State Listen

# 清掉所有残留后端进程（怀疑"假重启"时先清场）
Get-Process python | Stop-Process -Force

# 验证线上跨域是否放行（期望 200 + access-control-allow-origin 头）
curl.exe -s -i -X OPTIONS "https://954487onpr52.vicp.fun/api/v1/auth/login" -H "Origin: https://fabao.blog-x.cc.cd" -H "Access-Control-Request-Method: POST"

# 直接验证后端活着（期望返回 JSON）
curl.exe -s https://954487onpr52.vicp.fun/
```

## 10. 排错思路总结（下次自己也能查）

遇到"前端连不上后端"，按顺序问自己四个问题：

1. **请求打到哪了？** → F12 → Network → 找失败的那条请求看 URL
   - URL 是 CF Pages 自己的域名 → `VITE_API_BASE_URL` 没配/没重新部署（405 就是这么来的）
2. **服务通不通？** → 浏览器直接开 `https://后端地址/docs`，或 curl 一下
   - 打不开 → 花生壳/后端没在跑
3. **是不是跨域被拦？** → F12 → Console 看有没有 `blocked by CORS policy`，再用上面的 OPTIONS curl 验证
   - 返回 400 → `backend/.env` 白名单没这个域名，加上并**重启后端**
4. **配置真的生效了吗？** → 看后端启动日志那行 `CORS 允许来源：[...]`
   - 还是旧值 → 有残留实例，`Get-Process python | Stop-Process -Force` 清场后重启
