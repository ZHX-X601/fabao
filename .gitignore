# 本地工具脚本，不纳入版本控制
git-push-fabao.bat
start-backend.bat

# 本地环境变量文件（含 API 密钥，禁止提交）
.env.local

# ===== Python 后端（backend/）=====
# 虚拟环境
backend/.venv/
backend/.pythons/
# Python 缓存文件
__pycache__/
*.py[cod]
# 后端本地环境变量（含数据库密码，禁止提交）
backend/.env
# 上传的合同文件
backend/uploads/
# Pytest / 日志等本地产物
.pytest_cache/
*.log
